# 🚗 CarLeads CRM — CLAUDE.md
> AI-Powered Lead Management for Indian Car Dealers | Built by Webiox, Surat
> Version 2.0 — With Full Skills Ecosystem

---

## 🧠 HOW TO USE THIS FILE

Read this BEFORE writing any code. This is the single source of truth.
Every Claude Code session starts: "Read CLAUDE.md fully first."

---

## 📌 PROJECT OVERVIEW

**Product:** CarLeads CRM
**Tagline:** Ek inbox. Har platform. Sirf real buyers.
**Problem:** Dealers get 100 queries/day → 80 fake → waste 3-4 hrs on timepass
**Solution:** One CRM — all platforms in, AI scores leads, dealer only sees hot ones
**Market:** Used + new car dealers, starting Surat/Gujarat → pan-India
**Builder:** Webiox Agency

---

## 💰 BUSINESS MODEL

| Plan | Price | Limit |
|------|-------|-------|
| Basic | ₹1,999/month | 1 platform, 200 leads |
| Pro | ₹3,999/month | 3 platforms, unlimited |
| Premium | ₹6,999/month | All platforms + team login |

10 Basic dealers = ₹20,000 MRR. Target 50 dealers in 6 months.

---

## 🛠️ SKILLS SETUP (Install First — Before Coding)

### Step 1: Install Superpowers (obra/superpowers)
The core development methodology. Makes Claude Code follow TDD, write plans first, not jump into code.
```bash
/plugin install superpowers@claude-plugins-official
```
**What it gives us:**
- `brainstorming` — design before code
- `writing-plans` — detailed task breakdown
- `test-driven-development` — RED→GREEN→REFACTOR
- `subagent-driven-development` — parallel task execution
- `systematic-debugging` — 4-phase root cause process

### Step 2: Install UI/UX Intelligence (nextlevelbuilder/ui-ux-pro-max-skill)
Design system generator — no guessing colors/fonts/layouts.
```bash
npm install -g uipro-cli
uipro init --ai claude
```
**For our project, use this design system command:**
```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py \
  "car dealer CRM sales intelligence dashboard" \
  --design-system -p "CarLeads CRM" -f markdown \
  --persist
```
Style target: **Sales Intelligence Dashboard** + **Dark Mode OLED**

### Step 3: Install Awesome Skills (sickn33/antigravity-awesome-skills)
1,239+ skills. We use specific ones:
```bash
npx antigravity-awesome-skills --claude
```
**Skills to activate for this project:**
- `@architecture` — system design decisions
- `@api-design-principles` — REST API structure
- `@security-auditor` — webhook security, RLS
- `@test-driven-development` — testing patterns
- `@react-patterns` — Next.js component patterns
- `@brainstorming` — before any new feature
- `@debugging-strategies` — when stuck

### Step 4: NoxHD UI Components (Nox-HD/ui)
JS component library from digital marketing agency.
```bash
# Reference for component patterns — don't install, just use as reference
# https://github.com/Nox-HD/ui
```
Use for: card patterns, dashboard layouts, data display components.

---

## 🏗️ TECH STACK

| Layer | Tool | Why |
|-------|------|-----|
| Frontend | Next.js 14 (App Router) | SSR + fast |
| Styling | Tailwind CSS v3 + shadcn/ui | Dark mode, fast build |
| Backend | Node.js + Express | Simple, fast |
| DB | Supabase (Postgres) | Free tier, realtime, RLS |
| AI | Claude API (claude-sonnet-4-20250514) | Lead scoring |
| WhatsApp | Meta Cloud API | Free, official |
| Instagram | Meta Graph API | Free, official |
| Auth | Supabase Auth (phone/email) | Easy |
| Hosting | Vercel + Railway | ₹500/month total |
| Charts | Recharts | React-native |
| Icons | Lucide React | No emojis |

---

## 📁 PROJECT STRUCTURE

```
carleads-crm/
├── CLAUDE.md                        ← READ FIRST. Always.
├── .claude/
│   └── skills/
│       ├── ui-ux-pro-max/           ← Design intelligence (uipro-cli)
│       ├── brainstorming/           ← Superpowers skill
│       ├── writing-plans/           ← Superpowers skill
│       ├── test-driven-development/ ← Superpowers skill
│       ├── architecture/            ← Antigravity skill
│       ├── api-design-principles/   ← Antigravity skill
│       └── security-auditor/        ← Antigravity skill
├── design-system/
│   ├── MASTER.md                    ← Generated by uipro — DO NOT EDIT MANUALLY
│   └── pages/
│       ├── dashboard.md
│       ├── leads.md
│       └── settings.md
├── apps/
│   ├── web/                         ← Next.js 14 frontend
│   │   ├── app/
│   │   │   ├── layout.js
│   │   │   ├── page.js              ← Dashboard
│   │   │   ├── leads/page.js        ← Lead Inbox (PRIORITY #1)
│   │   │   ├── settings/page.js
│   │   │   └── login/page.js
│   │   ├── components/
│   │   │   ├── ui/                  ← shadcn components
│   │   │   ├── leads/
│   │   │   │   ├── LeadCard.jsx
│   │   │   │   ├── LeadList.jsx
│   │   │   │   ├── LeadDetail.jsx   ← Slide-over panel
│   │   │   │   ├── LeadFilters.jsx
│   │   │   │   └── ScoreBadge.jsx
│   │   │   ├── dashboard/
│   │   │   │   ├── StatsCard.jsx
│   │   │   │   ├── LeadsByPlatform.jsx  ← Donut chart
│   │   │   │   ├── ScoreDistribution.jsx ← Bar chart
│   │   │   │   ├── WeeklyTrend.jsx  ← Line chart
│   │   │   │   └── HotLeadsList.jsx
│   │   │   └── layout/
│   │   │       ├── Sidebar.jsx
│   │   │       ├── Header.jsx
│   │   │       └── MobileNav.jsx    ← Bottom tabs for phone
│   │   ├── lib/
│   │   │   ├── supabase.js
│   │   │   ├── api.js
│   │   │   └── utils.js
│   │   └── hooks/
│   │       ├── useLeads.js
│   │       ├── useRealtime.js       ← Supabase live updates
│   │       └── useStats.js
│   └── api/                         ← Express backend
│       ├── index.js
│       ├── routes/
│       │   ├── leads.js
│       │   ├── stats.js
│       │   └── webhooks/
│       │       ├── whatsapp.js      ← Meta webhook
│       │       └── instagram.js
│       ├── services/
│       │   ├── scorer.js            ← Claude API scoring
│       │   ├── whatsapp.js          ← Send messages
│       │   └── notifier.js          ← Hot lead alerts
│       └── middleware/
│           ├── auth.js
│           └── rateLimit.js
├── supabase/
│   └── migrations/
│       └── 001_initial_schema.sql
└── tests/                           ← TDD — write before code
    ├── scorer.test.js
    ├── webhook.test.js
    └── leads.test.js
```

---

## 🗄️ DATABASE SCHEMA

```sql
-- dealers
CREATE TABLE dealers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE,
  phone TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  shop_name TEXT,
  city TEXT DEFAULT 'Surat',
  plan TEXT DEFAULT 'basic' CHECK (plan IN ('basic','pro','premium')),
  whatsapp_connected BOOLEAN DEFAULT false,
  instagram_connected BOOLEAN DEFAULT false,
  whatsapp_phone_id TEXT,
  instagram_page_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- leads
CREATE TABLE leads (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  dealer_id UUID REFERENCES dealers(id) ON DELETE CASCADE,
  platform TEXT NOT NULL CHECK (platform IN ('whatsapp','instagram','manual','olx')),
  sender_name TEXT,
  sender_phone TEXT,
  sender_handle TEXT,
  message_preview TEXT,
  ai_score INTEGER CHECK (ai_score BETWEEN 1 AND 10),
  ai_tag TEXT CHECK (ai_tag IN ('hot','warm','fake')),
  ai_reason TEXT,
  ai_signals JSONB,
  status TEXT DEFAULT 'new' CHECK (status IN ('new','contacted','follow_up','converted','lost')),
  is_read BOOLEAN DEFAULT false,
  external_message_id TEXT UNIQUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- lead_messages
CREATE TABLE lead_messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  lead_id UUID REFERENCES leads(id) ON DELETE CASCADE,
  direction TEXT NOT NULL CHECK (direction IN ('inbound','outbound')),
  message_text TEXT,
  message_type TEXT DEFAULT 'text',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- notifications
CREATE TABLE notifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  dealer_id UUID REFERENCES dealers(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK (type IN ('hot_lead','follow_up_reminder','daily_summary')),
  title TEXT NOT NULL,
  body TEXT,
  lead_id UUID REFERENCES leads(id) ON DELETE SET NULL,
  is_read BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Indexes
CREATE INDEX idx_leads_dealer_score ON leads(dealer_id, ai_score DESC);
CREATE INDEX idx_leads_dealer_created ON leads(dealer_id, created_at DESC);
CREATE INDEX idx_leads_status ON leads(status);
CREATE INDEX idx_leads_external_id ON leads(external_message_id);

-- Row Level Security (IMPORTANT)
ALTER TABLE leads ENABLE ROW LEVEL SECURITY;
CREATE POLICY "dealer own leads" ON leads
  FOR ALL USING (dealer_id = auth.uid());
```

---

## 🤖 AI LEAD SCORING

**Model:** claude-sonnet-4-20250514
**Max tokens:** 200 (keep cost low)
**Timeout:** 3 seconds
**Fallback:** {score: 5, tag: "warm"} on failure

**System prompt:**
```
You are a lead qualification AI for Indian used/new car dealers.
Analyze this customer message and score buying intent.
Context: Indian market, Hinglish messages (Hindi+English mix) are normal.

Score 1-10:
- 1-3 (FAKE): "hello", "bhai price bata", random texts, no intent
- 4-6 (WARM): some interest but vague ("koi acchi car hai kya?")
- 7-10 (HOT): specific car + budget + location + urgency

Examples:
- "price?" → score 2
- "Swift 2019 chahiye, budget 4 lakh cash, Surat mein hoon" → score 9
- "hello hi" → score 1
- "Honda City 2022 ka price batao, 8 lakh budget hai" → score 8

Respond ONLY in JSON (no other text):
{"score": N, "tag": "hot|warm|fake", "reason": "brief reason", "signals": ["signal1"]}
```

**Hot lead alert to dealer (WhatsApp):**
```
🔥 Hot Lead Alert!

Name: {sender_name}
Message: {message_preview}
Score: {score}/10
Platform: {platform}

Reply now 👇
wa.me/{sender_phone}
```

---

## 🎨 DESIGN SYSTEM

**Run uipro to generate full design system:**
```bash
python3 .claude/skills/ui-ux-pro-max/scripts/search.py \
  "car dealer CRM sales dashboard dark mode" \
  --design-system -p "CarLeads" --persist
```

**Core tokens (until design-system/MASTER.md generated):**

| Token | Value | Use |
|-------|-------|-----|
| bg-primary | #0F1117 | Page background |
| bg-card | #1A1D27 | Cards, panels |
| bg-hover | #22252F | Hover states |
| border | #2A2D37 | All borders |
| text-primary | #F1F3F5 | Main text |
| text-secondary | #8B8F9A | Labels, hints |
| score-hot | #00D26A | Score 7-10 (green) |
| score-warm | #F5A623 | Score 4-6 (amber) |
| score-fake | #FF4757 | Score 1-3 (red) |
| accent | #6C5CE7 | CTAs, active states |
| font-body | DM Sans | All text |
| font-mono | DM Mono | Scores, numbers, stats |
| radius-card | 12px | Cards |
| radius-badge | 20px | Score badges |

**Rules (non-negotiable):**
- Dark mode ONLY
- Mobile-first (375px minimum)
- Lucide icons ONLY (no emojis as icons)
- DM Mono for ALL numbers
- No purple gradients (anti-pattern for B2B)
- No rounded bubbly cards (this is professional tool)

---

## 🚀 DEVELOPMENT WORKFLOW (Superpowers Method)

**Always follow this order:**

```
1. BRAINSTORM  → Use @brainstorming skill before any new feature
2. DESIGN      → Generate design system via uipro
3. PLAN        → Use @writing-plans skill — tasks must be 2-5 min each
4. TEST FIRST  → Write failing test BEFORE writing code (TDD)
5. CODE        → Write minimal code to pass test
6. REVIEW      → Use @requesting-code-review skill
7. COMMIT      → Clean commit with clear message
```

**Never skip brainstorm → plan → test → code order.**
Superpowers enforces this automatically once installed.

---

## 📱 SCREENS — BUILD ORDER

1. **Lead Inbox** `/leads` ← BUILD FIRST. Core value prop.
2. **Dashboard** `/` ← Stats and charts
3. **Lead Detail** (slide-over panel) ← Opens from inbox
4. **Settings** `/settings` ← Platform connections
5. **Login** `/login` ← Auth

---

## 🔌 INTEGRATIONS

### WhatsApp (Meta Cloud API)
- Verify token: `WEBHOOK_VERIFY_TOKEN=carleads_webhook_2024`
- Webhook GET: verify hub.challenge
- Webhook POST: incoming messages
- Signature: verify `X-Hub-Signature-256`
- Send: POST to `https://graph.facebook.com/v17.0/{PHONE_ID}/messages`

### Instagram (Meta Graph API)
- Webhook POST: `/webhooks/instagram`
- Event: `messages`

### Claude API
- Endpoint: `https://api.anthropic.com/v1/messages`
- Model: `claude-sonnet-4-20250514`
- max_tokens: 200
- Purpose: lead scoring only

### Supabase Realtime
- Subscribe to `leads` table inserts per dealer
- New hot lead → UI updates instantly without refresh

---

## 🔐 SECURITY (Use @security-auditor skill before shipping)

- Supabase RLS on ALL tables (dealer sees only own data)
- WhatsApp webhook: verify HMAC SHA-256 signature
- Rate limit: 100 req/min on all public endpoints
- JWT auth on all API routes
- Never log full message content in prod
- Env vars: never hardcode, always `.env.local`

---

## ✅ MVP SCOPE

**Build (Phase 1):**
- [ ] Auth (phone/email login)
- [ ] WhatsApp webhook → leads inbox
- [ ] Instagram webhook → leads inbox
- [ ] Manual lead entry
- [ ] AI lead scoring
- [ ] Lead inbox (score-sorted, filterable)
- [ ] Lead detail + notes
- [ ] Dashboard stats
- [ ] WhatsApp quick reply from dashboard
- [ ] Hot lead WhatsApp alert to dealer

**Skip (Phase 2+):**
- OLX scraping
- JustDial integration
- Team login
- Mobile app
- PDF reports
- Inventory management

---

## 📋 CODE CONVENTIONS

- JavaScript (not TypeScript) — Indian dev team preference
- `{ success, data, error }` response format everywhere
- camelCase JS, snake_case DB
- One component per file, export default
- All API routes: `/api/v1/...`
- Comments in English, short

---

## 💡 IMPORTANT CONTEXT

1. **Indian market** — Hinglish normal, ₹ for currency, WhatsApp is primary
2. **Mobile-first** — Dealers check on phones. 375px must work perfectly
3. **Speed** — Hot lead alert within 30 seconds of message receipt
4. **Simple UI** — Dealers not tech-savvy. Zero jargon
5. **WhatsApp > email** — All alerts via WhatsApp

---

## 🛠️ ENV VARS

```env
# apps/web/.env.local
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=http://localhost:3001

# apps/api/.env
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
ANTHROPIC_API_KEY=
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WEBHOOK_VERIFY_TOKEN=carleads_webhook_2024
INSTAGRAM_TOKEN=
INSTAGRAM_PAGE_ID=
PORT=3001
```

---

## 🏃 QUICK START

```bash
# Install skills (do once)
/plugin install superpowers@claude-plugins-official
npx antigravity-awesome-skills --claude
npm install -g uipro-cli && uipro init --ai claude

# Generate design system (do once)
python3 .claude/skills/ui-ux-pro-max/scripts/search.py \
  "car dealer crm sales dashboard" --design-system -p "CarLeads" --persist

# Start dev
cd apps/web && npm run dev
cd apps/api && npm run dev
```

---

*This CLAUDE.md is the single source of truth. Read fully before every session.*
*Last updated: CarLeads CRM v2.0 — Webiox Agency, Surat*
